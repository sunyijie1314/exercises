/*1080 静态库链接
Linux静态方式链接，链接静态库的先后顺序（优先级）会影响程序能否链接成功。它是按照MakeFile当中的指定的顺序进行先后链接的。
链接器维护了两个集合，一个是已定义符号集合D，另一个是未定义符号集合U。链接器会依次扫描依赖的静态库包含的每个目标文件，扫描过程如下：
1、对于当前目标文件，查找其符号表，将每一个当前目标文件已定义的符号与未定义符号集U进行比较，如果有交集那么将.o链接进来，将定义的符号加到已定义符号集合D中（同时删除U中相应符号）如果无交集直接丢弃。
2、对于当前目标文件，查找其符号表，将每一个当前目标文件引用的符号与已定义符号集合D进行对比，如果该符号不在集合D中则将其添加到未定义符合集合U中。
经过上述过程，不断更新符号集D和符号集U。当所有文件都扫描完成后，如果未定义符号集合U不为空，则说明当前输入的目标文件集合中有未定义错误，链接器报错，整个编译过程终止
说明：每个静态库只会链接一次，防止递归重复链接。
通过上面过程可以知道：
		1 静态库链接顺序指定错误会报查找不到符号的链接错误。
		2 多个库拥有相同符号，谁先链接到进入符号集D中就用谁的。
举例1：假设有个程序，它的MakeFile当中的静态链接顺序如下：
